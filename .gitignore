node_modules/
.next/
coverage/
.DS_Store
*.log
.env*
!.env.example
!.env.dokploy.example

/generated/prisma

# TypeScript build cache (regenerated on every build)
/tsconfig.tsbuildinfo

# Uploaded letterhead images (regenerated at runtime)
/uploads/letterheads/*
!/uploads/letterheads/.gitkeep

certificates
/certs

# Test/debug scripts (moved to tests/)
tests/

# Database backups and raw PostgreSQL data (production data, do not commit)
/_data/
*.sql

# Temporary recovery compose file (keep local only)
recovery-docker-compose.yml
